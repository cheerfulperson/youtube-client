import { CustomPswValidatorDirective } from './custom-psw-validator.directive';

describe('CustomPswValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPswValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
